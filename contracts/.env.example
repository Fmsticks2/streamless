# Massa Wallet Private Key for Contract Deployment
# Replace with your actual private key that has sufficient MAS tokens for deployment
PRIVATE_KEY=your_private_key_here

# Network Configuration (buildnet is the testnet)
NETWORK=buildnet

# Optional: Custom RPC endpoint (defaults to buildnet if not specified)
# JSON_RPC_URL=https://buildnet.massa.net/api/v2